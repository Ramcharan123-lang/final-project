
  # std_project_manage

  This is a code bundle for std_project_manage. The original project is available at (http://localhost:3000/)

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
